# The lab has a home — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rename `aleogr/shared-infra` to `aleogr/lab` without CI ever losing its ability to authenticate, publish an index page at `lab.aleogr.dev`, and move the laboratory's own rules and its earlier spec into the repository that now owns them.

**Architecture:** The federation's trust is widened to accept both repository names while CI still works, the repository is renamed, and the trust is narrowed afterwards — so no step depends on an authentication the step itself is repairing. The page is hand-written HTML at the root of a `site` branch, published by GitHub Pages deploying from a branch, reading each project's public `/health` from the reader's browser. Nothing in this plan gives a workflow, a page or a build any credential it does not already hold.

**Tech Stack:** Terraform (`hashicorp/google ~> 8.3`, Terraform `~> 1.16.0`), GitHub Actions, GitHub Pages, Cloudflare DNS, `gcloud`, `bash`, and — in the marketplace only — Go.

**Spec:** `docs/superpowers/specs/2026-09-21-lab-home-design.md`

**Status (2026-09-21):** Tasks 1, 2, 3 and 6 are complete, each merged as its
own pull request — that pull request is the record, not this file. Task 4 is
complete: the page is live at `https://lab.aleogr.dev` with its certificate
issued. Task 5 is this work, in progress. The steps below stay unchecked; for
the completed tasks they describe how the work was actually done, not work
still waiting. Quoted content inside those completed tasks' steps carries
corrections made after each task shipped, so it agrees with the file it
produced; a difference between what a step quotes and what a later step or
another document says is a mistake to fix, not a second decision to weigh.

## Global Constraints

- **Everything versioned is in English.** Code, comments, commit messages, pull request titles and bodies, documentation, and the page.
- **The page has no build, no dependency and no publishing workflow** (spec D4). `actions/deploy-pages` requires `id-token: write`, the same permission that mints this project's GCP credential.
- **Third-party actions are pinned to a commit SHA, never to a tag.**
- **No secret is ever committed.** This repository is public.
- **Model identifiers appear only in the attribution trailer** of commit messages and pull request bodies — never in code, documentation, titles or comments.
- **Terraform is applied only by CI**, from `main`, on push.
- **Claude Code never approves and never merges.** The owner merges.
- **Manual steps go to the owner one at a time**, and the next instruction waits for the result of the previous one.
- **A green plan is not a green apply.** Where a step's proof is a permission, the proof is the log of an apply or a check running as the real identity — never the configuration.

## File structure

| File | Responsibility |
|---|---|
| `gcp/tools/check-federation.sh` | **new.** Reads the live workload identity provider and the deploy account's live bindings and asserts exactly which repositories may authenticate. |
| `gcp/terraform/deployer.tf` | **modified.** The trusted repositories become one list; the provider's condition and the `workloadIdentityUser` bindings are both derived from it. |
| `.github/workflows/ci.yml` | **modified.** Runs `check-federation.sh` after the apply, naming the repositories it expects. |
| `README.md` | **modified.** Describes the laboratory rather than one of the things it holds. |
| `docs/lab.md` | **new.** The rules that hold across the whole laboratory. |
| `docs/superpowers/specs/2026-09-20-shared-database-instance-design.md` | **moved in** from `aleogr/marketplace`. |
| `index.html`, `CNAME` | **new, on the `site` branch only.** The page and the domain Pages serves it under. |
| `internal/platform/httpx/httpx.go` (marketplace) | **modified.** `/health` answers with `Access-Control-Allow-Origin`. |

---

### Task 1: The federation accepts both names, and a check that reads the live one

**Files:**
- Create: `gcp/tools/check-federation.sh`
- Modify: `gcp/terraform/deployer.tf`
- Modify: `.github/workflows/ci.yml`

**Interfaces:**
- Consumes: nothing from earlier tasks.
- Produces: `gcp/tools/check-federation.sh <project> <repository>...`, exit 0 when the live federation names exactly those repositories and nothing else, exit 1 otherwise. Task 3 re-runs it with one argument instead of two. `local.federated_repositories` in `deployer.tf` is the list Task 3 shortens.

- [ ] **Step 1: Write the check**

Create `gcp/tools/check-federation.sh`:

```bash
#!/usr/bin/env bash
# Which repositories may authenticate as the deploy identity, asserted against
# the LIVE federation.
#
# THE EXPECTED NAMES ARE ARGUMENTS AND NOT READ FROM THE CONFIGURATION. A check
# that takes its expectation from the thing it is checking proves nothing. These
# come from the workflow; `deployer.tf` declares its own list; and the two
# disagreeing is the failure this exists to catch.
#
# It exists because a rename widens this trust and something has to narrow it
# again (docs/superpowers/specs/2026-09-21-lab-home-design.md, D3 and D10). A
# widening nobody removed looks exactly like a widening nobody made.
set -euo pipefail

project="${1:?usage: check-federation.sh <project> <repository>...}"
shift
if [ "$#" -eq 0 ]; then
  echo "usage: check-federation.sh <project> <repository>..." >&2
  exit 2
fi

expected="$(printf '%s\n' "$@" | sort)"
failures=0

number="$(gcloud projects describe "$project" --format='value(projectNumber)')"

condition="$(gcloud iam workload-identity-pools providers describe github \
  --project="$project" --location=global --workload-identity-pool=github \
  --format='value(attributeCondition)')"

# Every repository the condition names, and then whatever is left once the
# names and the separators are removed — because a clause nobody planned is
# exactly what this must not let through.
named="$(printf '%s' "$condition" \
  | grep -oE "assertion\.repository == '[^']+'" \
  | sed -E "s/.*'([^']+)'.*/\1/" | sort || true)"
remainder="$(printf '%s' "$condition" \
  | sed -E "s/assertion\.repository == '[^']+'//g" \
  | sed -E 's/\|\|//g' | tr -d '[:space:]')"

if [ "$named" != "$expected" ]; then
  echo "the provider's condition names:"
  printf '%s\n' "$named" | sed 's/^/  /'
  echo "expected:"
  printf '%s\n' "$expected" | sed 's/^/  /'
  failures=$((failures + 1))
fi

if [ -n "$remainder" ]; then
  echo "the condition carries something beyond the repository names: $remainder"
  failures=$((failures + 1))
fi

members="$(gcloud iam service-accounts get-iam-policy \
  "deployer@$project.iam.gserviceaccount.com" --project="$project" --format=json \
  | python3 -c "
import json, sys

policy = json.load(sys.stdin)
for binding in policy.get('bindings', []):
    if binding.get('role') == 'roles/iam.workloadIdentityUser':
        for member in binding.get('members', []):
            print(member)
" | sort)"

wanted="$(for repository in "$@"; do
  echo "principalSet://iam.googleapis.com/projects/$number/locations/global/workloadIdentityPools/github/attribute.repository/$repository"
done | sort)"

if [ "$members" != "$wanted" ]; then
  echo "the deploy account trusts:"
  printf '%s\n' "$members" | sed 's/^/  /'
  echo "expected:"
  printf '%s\n' "$wanted" | sed 's/^/  /'
  failures=$((failures + 1))
fi

if [ "$failures" -ne 0 ]; then
  echo "the federation does not match the design"
  exit 1
fi

echo "the federation matches the design"
```

Then: `chmod +x gcp/tools/check-federation.sh`

- [ ] **Step 2: Run it and watch it fail — this is the red**

This runs in the owner's Cloud Shell, because a session has no `gcloud`
credential. Give them exactly this, and nothing else in the same message:

```sh
rm -rf ~/lab-check && git clone --depth 1 -b claude/lab-federation \
  https://github.com/aleogr/shared-infra ~/lab-check \
  && ~/lab-check/gcp/tools/check-federation.sh \
       aleogr-lab-shared-dacd aleogr/shared-infra aleogr/lab
```

Expected: **exit 1**, with output naming only `aleogr/shared-infra` where two
were expected. Record the output in the pull request: it is the evidence that
the check can fail, which is the only thing that makes its later success mean
anything.

If it fails instead with a permission error, that is the blind spot `README.md`
already documents — the owner can read what the deploy identity cannot. Grant
for the resource and declare the grant; do not narrow the check to fit the
permission.

- [ ] **Step 3: Widen the configuration**

In `gcp/terraform/deployer.tf`, add this above `google_service_account.deployer`:

```hcl
# THE REPOSITORIES THIS PROJECT TRUSTS, in the one list that says so.
#
# Two names is a transient state, not the design. The condition is widened
# here, the repository is renamed on GitHub, and the old name is removed
# afterwards (docs/superpowers/specs/2026-09-21-lab-home-design.md, D3). The
# order is not a preference: a CI run that cannot authenticate cannot apply the
# fix for not being able to authenticate.
locals {
  federated_repositories = [
    "aleogr/shared-infra",
    "aleogr/lab",
  ]
}
```

Replace the provider's condition:

```hcl
  attribute_condition = join(" || ", [
    for repository in local.federated_repositories :
    "assertion.repository == '${repository}'"
  ])
```

Replace `google_service_account_iam_member.deployer_federation` entirely:

```hcl
resource "google_service_account_iam_member" "deployer_federation" {
  for_each = toset(local.federated_repositories)

  service_account_id = google_service_account.deployer.name
  role               = "roles/iam.workloadIdentityUser"
  member             = "principalSet://iam.googleapis.com/projects/${data.google_project.current.number}/locations/global/workloadIdentityPools/${google_iam_workload_identity_pool.github.workload_identity_pool_id}/attribute.repository/${each.value}"
}

# ONE RESOURCE BECAME ONE PER REPOSITORY, and without this Terraform would
# destroy the binding CI authenticates with and create it again in the same
# apply. An apply that failed between those two steps would leave CI unable to
# authenticate, with no way in to repair it.
moved {
  from = google_service_account_iam_member.deployer_federation
  to   = google_service_account_iam_member.deployer_federation["aleogr/shared-infra"]
}
```

- [ ] **Step 4: Run the check in CI, after the apply**

In `.github/workflows/ci.yml`, after the `The instance matches the design` step:

```yaml
      # The same reason as the step above: the condition that matters is the one
      # the live provider holds, not the one the files describe.
      - name: The federation matches the design
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          ./gcp/tools/check-federation.sh "${{ vars.GCP_PROJECT_ID }}" \
            aleogr/shared-infra aleogr/lab
```

- [ ] **Step 5: Prove the configuration is well-formed before pushing**

```bash
cd gcp/terraform
terraform fmt -check -recursive
terraform init -backend=false -input=false
terraform validate
```

Expected: `fmt` silent, `validate` prints `Success!`.

- [ ] **Step 6: Commit and open the pull request**

```bash
git checkout -b claude/lab-federation
git add gcp/tools/check-federation.sh gcp/terraform/deployer.tf .github/workflows/ci.yml
git commit
git push -u origin claude/lab-federation
```

The pull request body carries the failing output from Step 2 and says what the
plan must show: the provider **updated in place**, one binding **added**, and —
because of the `moved` block — **no binding destroyed**.

- [ ] **Step 7: Read the plan in the job log before asking for a merge**

Open the `Plan` step of the CI run. Confirm, in the text:

- `google_iam_workload_identity_pool_provider.github` will be **updated in place**
- `google_service_account_iam_member.deployer_federation["aleogr/lab"]` will be **created**
- `google_service_account_iam_member.deployer_federation["aleogr/shared-infra"]` appears **only** under the `moved` notice, not as a destroy

A destroy of the existing binding means the `moved` block is wrong. Stop, say so,
and do not ask for the merge.

- [ ] **Step 8: Owner merges; then read the apply and the check**

In the run on `main`: `Apply complete!` with one resource added and none
destroyed, and `The federation matches the design` printing exactly that.

- [ ] **Step 9: The green — run the check again by hand**

The owner runs the same command as Step 2. Expected now: exit 0, `the federation
matches the design`. Red before, green after, same command, same script.

---

### Task 2: The repository is renamed

**Files:** none. This task changes GitHub, not the tree.

**Interfaces:**
- Consumes: the widened federation from Task 1, applied and verified.
- Produces: the repository reachable as `aleogr/lab`; every later task's pull requests are opened there.

- [ ] **Step 1: Confirm Task 1 landed before touching anything**

`The federation matches the design` must have passed on `main` (Task 1, Step 8).
If it has not, stop: renaming now is the failure this whole sequence was built
to avoid.

- [ ] **Step 2: Owner renames the repository**

One manual step, given on its own:

> GitHub → `aleogr/shared-infra` → Settings → General → Repository name →
> `lab` → Rename.

- [ ] **Step 3: Prove CI still authenticates under the new name**

The rename does not itself start a workflow. Start one by pushing the branch
for Task 3, and read that run's `Init` and `Plan` steps.

Expected: the run authenticates and the plan succeeds. If it fails at the
`google-github-actions/auth` step, Task 1 was incomplete; the way back is to
rename the repository to `shared-infra` again, which restores the previous
state exactly.

- [ ] **Step 4: Point the local clone at the new name**

GitHub redirects the old URL, so this is tidiness rather than repair:

```bash
git remote set-url origin https://github.com/aleogr/lab
git remote -v
```

---

### Task 3: The federation names one repository again

**Files:**
- Modify: `gcp/terraform/deployer.tf`
- Modify: `.github/workflows/ci.yml`
- Modify: `README.md`

**Interfaces:**
- Consumes: `local.federated_repositories` and `check-federation.sh` from Task 1.
- Produces: a federation that trusts `aleogr/lab` alone.

- [ ] **Step 1: Shorten the list**

In `gcp/terraform/deployer.tf`:

```hcl
locals {
  federated_repositories = [
    "aleogr/lab",
  ]
}
```

Remove the `moved` block — it described a migration that has happened, and a
`moved` block whose source no longer exists is noise for the next reader.

Rewrite the comment above the list, because the transient state is over:

```hcl
# THE REPOSITORIES THIS PROJECT TRUSTS, in the one list that says so.
#
# One name, and the check in CI asserts the live provider names exactly that.
# The list was briefly two while this repository was renamed
# (docs/superpowers/specs/2026-09-21-lab-home-design.md, D3).
```

- [ ] **Step 2: Correct the deploy identity's description**

In the same file, `google_service_account.deployer` still says
`Federated to aleogr/shared-infra`. Change it to `aleogr/lab`. A repository's
first lie about itself is the one nobody edited.

- [ ] **Step 3: Narrow what CI expects**

In `.github/workflows/ci.yml`:

```yaml
      - name: The federation matches the design
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: ./gcp/tools/check-federation.sh "${{ vars.GCP_PROJECT_ID }}" aleogr/lab
```

- [ ] **Step 4: Rewrite the README's opening**

Replace the title and the first paragraph:

```markdown
# lab

The home of `lab.aleogr.dev`: the Cloud SQL instance the laboratory's projects
share, the index page the address answers with, and the rules that hold across
every project in it.

## What lives here

What is **true about the laboratory as a whole**. What is true about one project
lives in that project. The sleep schedule is the laboratory's, because stopping
the instance affects everyone; the `marketplace` database's connection ceiling
is the marketplace's, because its own pool is what sets the number.

Without that rule, "it belongs to the lab" becomes the answer for anything
nobody knows where to put.
```

Leave `What this does not own` exactly as it is. Its sentence about the house
and the rooms is the boundary this repository is built on.

- [ ] **Step 5: Validate, commit, open the pull request**

```bash
cd gcp/terraform && terraform fmt -check -recursive && terraform validate
cd - && git checkout -b claude/lab-narrow
git add gcp/terraform/deployer.tf .github/workflows/ci.yml README.md
git commit
git push -u origin claude/lab-narrow
```

- [ ] **Step 6: Read the plan before the merge**

Expected: the provider **updated in place**, and
`google_service_account_iam_member.deployer_federation["aleogr/shared-infra"]`
**destroyed**. That destroy is the point of this task. Nothing else changes.

- [ ] **Step 7: Owner merges; the check is the proof**

`The federation matches the design` passes on `main` with one repository
expected. If it fails naming two, the apply did not remove the old binding, and
the trust is still wider than the design.

---

### Task 4: The page

**Files:**
- Create, on the `site` branch only: `index.html`, `CNAME`, `.nojekyll`
- Manual: GitHub Pages settings, Cloudflare DNS

**Interfaces:**
- Consumes: nothing from earlier tasks. It can be built before Task 3 lands; it cannot be published before Task 2, because Pages is configured on the renamed repository.
- Produces: `https://lab.aleogr.dev`. Task 6 makes the marketplace's card read as up instead of unknown.

- [ ] **Step 1: Create the orphan branch**

An orphan branch, not a copy of `main`: Pages serves this branch's root, and
nothing on `main` belongs at the laboratory's front door.

```bash
git checkout --orphan site
git rm -rf .
```

- [ ] **Step 2: Write the page**

Create `index.html`:

```html
<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="robots" content="noindex">
<title>lab.aleogr.dev</title>
<style>
  :root {
    color-scheme: light dark;
    --bg: #fbfbfa; --fg: #1a1a18; --muted: #6b6b66;
    --line: #e4e4e0; --card: #ffffff;
    --up: #1a7f4b; --down: #b4322a; --unknown: #8a8a84;
  }
  @media (prefers-color-scheme: dark) {
    :root {
      --bg: #16161a; --fg: #ecece8; --muted: #9a9a94;
      --line: #2a2a30; --card: #1d1d22;
      --up: #4cc38a; --down: #e5644f; --unknown: #78787e;
    }
  }
  * { box-sizing: border-box; }
  body {
    margin: 0; padding: 3rem 1rem 4rem; background: var(--bg); color: var(--fg);
    font: 16px/1.6 ui-sans-serif, system-ui, -apple-system, "Segoe UI", sans-serif;
  }
  main { max-width: 46rem; margin: 0 auto; }
  h1 { font-size: 1.5rem; letter-spacing: -0.01em; margin: 0 0 .25rem; }
  h2 { font-size: .8rem; text-transform: uppercase; letter-spacing: .08em;
       color: var(--muted); margin: 2.5rem 0 .75rem; font-weight: 600; }
  p.lede { color: var(--muted); margin: 0 0 1rem; }
  .card { background: var(--card); border: 1px solid var(--line);
          border-radius: 10px; padding: 1rem 1.15rem; margin-bottom: .75rem; }
  .card h3 { margin: 0; font-size: 1rem; display: flex; align-items: baseline; gap: .6rem; }
  .card p { margin: .35rem 0 .6rem; color: var(--muted); font-size: .9rem; }
  .dot { width: .6rem; height: .6rem; border-radius: 50%; flex: none;
         background: var(--unknown); align-self: center; }
  .dot[data-state="up"] { background: var(--up); }
  .dot[data-state="down"] { background: var(--down); }
  .state { font-size: .8rem; color: var(--muted); font-weight: 400; margin-left: auto; }
  ul { list-style: none; margin: 0; padding: 0; display: flex;
       flex-wrap: wrap; gap: .4rem .9rem; font-size: .9rem; }
  a { color: inherit; text-decoration-color: var(--line); text-underline-offset: 3px; }
  a:hover { text-decoration-color: currentColor; }
  footer { margin-top: 3rem; color: var(--muted); font-size: .85rem; }
  code { font: .85em ui-monospace, SFMono-Regular, Menlo, monospace; }
  @media (prefers-reduced-motion: no-preference) {
    .dot { transition: background-color .2s ease; }
  }
</style>
</head>
<body>
<main>
  <h1>lab.aleogr.dev</h1>
  <p class="lede">Everything under this address is a laboratory. Nothing here is production.</p>

  <h2>Projects</h2>
  <div id="projects"></div>

  <h2>The shared database</h2>
  <div class="card">
    <h3>lab-postgres</h3>
    <p><code>aleogr-lab-shared-dacd:us-central1:lab-postgres</code> — one PostgreSQL 16
       instance, one database per project.</p>
    <p>It runs continuously today. The intention is to let it sleep four
       weeknights — Monday to Thursday, 22:00–07:45 (UTC−3) — leaving Friday
       night and the whole weekend awake, and that is not in effect yet. When
       it is, a project outside the window will answer that its database is
       unreachable, and that will be expected rather than broken.</p>
  </div>

  <footer>
    <a href="https://github.com/aleogr/lab">aleogr/lab</a> — the instance, this page,
    and the rules that hold across the laboratory.
  </footer>
</main>

<script>
// THE INVENTORY. Edited by hand; there are three projects, not three hundred.
// `health` is the public endpoint this page reads. A project without one reads
// as unknown, which is the honest answer and not a failure.
const PROJECTS = [
  {
    name: "marketplace",
    about: "A multi-tenant marketplace platform.",
    repository: "https://github.com/aleogr/marketplace",
    hosts: ["https://marketplace.lab.aleogr.dev"],
    health: "https://marketplace.lab.aleogr.dev/health",
  },
  {
    name: "schooling",
    about: "A course platform and its console.",
    repository: "https://github.com/codeschool-ing/schooling",
    hosts: [
      "https://code.schooling.lab.aleogr.dev",
      "https://my.schooling.lab.aleogr.dev",
      "https://console.schooling.lab.aleogr.dev",
      "https://math.schooling.lab.aleogr.dev",
    ],
    health: null,
  },
];

// THREE STATES, NEVER TWO. "unknown" is what a page says when it could not
// check — a panel that calls that a failure cries wolf, and one that calls it
// success hides a fire.
async function probe(project) {
  if (!project.health) return { state: "unknown", detail: "no health endpoint" };
  const stop = new AbortController();
  const timer = setTimeout(() => stop.abort(), 8000);
  try {
    const response = await fetch(project.health, { signal: stop.signal, cache: "no-store" });
    // An answer that arrived is an answer, whatever shape it came in. Only a
    // body that parses adds detail; one that does not still tells us the
    // service replied, and with what status.
    let body = {};
    try { body = await response.json(); } catch (_) { /* not JSON, still an answer */ }
    const parts = [];
    if (body.database) parts.push(`database ${body.database}`);
    if (body.version) parts.push(body.version);
    return response.ok
      ? { state: "up", detail: parts.join(" · ") }
      : { state: "down", detail: [String(response.status), ...parts].join(" · ") };
  } catch {
    // A cross-origin refusal and an unreachable host are the same rejection
    // here, and neither of them means the service is down.
    return { state: "unknown", detail: "could not be read from this page" };
  } finally {
    clearTimeout(timer);
  }
}

// Cards are built and appended in inventory order, up front, each starting
// unknown — a probe updates only its own card in place when it resolves, so
// the list never reshuffles itself while the network answers arrive.
function createCard(project) {
  const card = document.createElement("div");
  card.className = "card";

  const heading = document.createElement("h3");
  const dot = document.createElement("span");
  dot.className = "dot";
  dot.dataset.state = "unknown";
  heading.append(dot, project.name);

  const state = document.createElement("span");
  state.className = "state";
  state.textContent = "unknown";
  heading.append(state);

  const about = document.createElement("p");
  about.textContent = project.about;

  const links = document.createElement("ul");
  for (const host of project.hosts.concat(project.repository)) {
    const item = document.createElement("li");
    const link = document.createElement("a");
    link.href = host;
    link.textContent = host.replace(/^https:\/\//, "");
    item.append(link);
    links.append(item);
  }

  card.append(heading, about, links);
  return { card, dot, state };
}

function applyStatus(refs, status) {
  refs.dot.dataset.state = status.state;
  refs.state.textContent = status.detail ? `${status.state} · ${status.detail}` : status.state;
}

const container = document.getElementById("projects");
for (const project of PROJECTS) {
  const refs = createCard(project);
  container.append(refs.card);
  probe(project).then((status) => applyStatus(refs, status));
}
</script>
</body>
</html>
```

- [ ] **Step 3: Tell Pages the domain, and Jekyll to stay out**

```bash
echo 'lab.aleogr.dev' > CNAME
touch .nojekyll
```

`.nojekyll` stops GitHub running the site through Jekyll, which is a build this
design does not want and does not need.

- [ ] **Step 4: Check the page before publishing it**

```bash
python3 -m http.server 8000 & SERVER=$!
sleep 1
curl -sS -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8000/
kill "$SERVER"
```

Expected: `200`. Then open it in a browser and confirm: both cards render, the
marketplace reads **unknown — could not be read from this page** (Task 6 has not
happened yet, and this is the three-state rule working rather than failing), and
the layout holds at a phone's width.

- [ ] **Step 5: Commit and push the branch**

```bash
git add index.html CNAME .nojekyll
git commit
git push -u origin site
```

This branch is never merged into `main`, and `main` is never merged into it.

- [ ] **Step 6: Owner turns Pages on**

One manual step:

> GitHub → `aleogr/lab` → Settings → Pages → Source: **Deploy from a branch** →
> Branch: `site`, folder `/ (root)` → Save.

Confirm the source reads "Deploy from a branch". If it reads "GitHub Actions",
the setting is wrong and a workflow will run — which spec D4 forbids.

- [ ] **Step 7: Read GitHub's documentation for what a subdomain needs — never from memory**

Open GitHub's documentation on configuring a custom domain for Pages and
confirm what it asks for a **subdomain**, as opposed to an apex: a single
`CNAME` record naming `<owner>.github.io` — `aleogr.github.io` here — not the
`A` records it asks for at an apex. **Confirm this at the moment of applying,
not from memory or from an earlier assumption**: an early version of this
plan assumed the wrong record type instead of checking the documentation, and
spec D9 records why that assumption was wrong.

- [ ] **Step 8: Owner adds the DNS record**

One manual step, in Cloudflare, zone `aleogr.dev`:

> Add one `CNAME` record named `lab`, target `aleogr.github.io`, proxy status
> **DNS only**.

`lab.aleogr.dev` already carries the Brevo verification `TXT`, which sustains
the laboratory's mail; leave it exactly where it is. The general DNS rule says
a CNAME cannot coexist with another record at the same name, but Cloudflare
does not enforce that here, and the two coexist without conflict (spec D9). If
a future provider does enforce it, the fallback is `A` records to GitHub's
published Pages addresses, read fresh at that time — never from memory or from
this plan, because those addresses are exactly the class of fact that changes.

- [ ] **Step 9: Prove the page answers, and over HTTPS**

```bash
dig +short lab.aleogr.dev
curl -sS -o /dev/null -w '%{http_code}\n' https://lab.aleogr.dev/
```

Expected: the name resolves through the `CNAME` to GitHub's Pages addresses,
then `200`. The certificate is issued by GitHub after the record propagates —
minutes usually, hours sometimes. Until it is issued, HTTPS fails and that is
not a fault to chase.

Then, in Settings → Pages, tick **Enforce HTTPS**, which only becomes available
once the certificate exists.

- [ ] **Step 10: Verify the domain, so nobody else can claim the subdomain**

An unverified custom domain can be claimed by another GitHub account if this
repository ever stops serving it. Verifying `aleogr.dev` covers every name
beneath it, including this one.

> GitHub → account Settings → Pages → **Add a domain** → `aleogr.dev` → it
> shows a `TXT` record name and value.

Then, one manual step in Cloudflare:

> Add the `TXT` record exactly as GitHub printed it, proxy status **DNS only**.

Return to GitHub and press **Verify**. Expected: the domain is listed as
verified. This adds a record at `_github-pages-challenge-aleogr.aleogr.dev`, a
different name from `lab.aleogr.dev`, so it does not touch the Brevo `TXT`.

---

### Task 5: The laboratory's rules, and the spec that was in the wrong place

**Files:**
- Create: `docs/lab.md` (on `main`)
- Modify: `README.md`, `CLAUDE.md`, `.github/workflows/ci.yml`
- Create: `docs/superpowers/specs/2026-09-20-shared-database-instance-design.md` (moved in)
- In `aleogr/marketplace`: delete that spec and correct what pointed at it

**Interfaces:**
- Consumes: the README opening written in Task 3.
- Produces: one place that answers "what are the rules of this laboratory".

- [ ] **Step 1: Write the rules down**

Create `docs/lab.md` on `main`:

```markdown
# The laboratory

What is true across every project under `lab.aleogr.dev`. What is true about one
project lives in that project.

## The rule that decides what may be shared

**Consolidate by environment, never across environments.** Every database in
`lab-postgres` holds a laboratory. The day one of them becomes production, it
leaves.

See `docs/superpowers/specs/2026-09-20-shared-database-instance-design.md` for
why, including what this arrangement does *not* protect against.

## Names

| | |
|---|---|
| Project | `aleogr-lab-<product>-<4 random>`, `aleogr-prd-<product>-<4 random>` — environment first, so every laboratory and every production project sorts together in the console and in `gcloud projects list`, putting the thing you must not confuse at the front of the name. Ids are immutable and a deleted one never returns, so everything that cannot be fixed later is in the name from the start |
| Shared project | `aleogr-lab-shared-<4 random>` |
| Shared instance | `lab-postgres` — named for what it is, never for the first project that used it |
| Database | the project's own name |
| Database role | the project's name as a prefix: `marketplace_migrator`, not `migrator`. Roles are cluster-wide objects, so a generic name on a shared instance is a collision waiting for the second tenant |

## When the instance is awake

**Not yet — the instance runs continuously today.** Plan 2 puts it to sleep on
four weeknights only, Monday through Thursday, from 22:00 to 07:45 local
(UTC−3); 07:45 rather than 08:00 because a stopped instance takes a minute or
two to accept connections. Friday night and the whole weekend stay awake: two
hours until Saturday is not worth a stop and a start, and the same risk holds
for Sunday night, which would leave the database dying at midnight while
somebody is working.

Two consequences, and both have bitten somebody:

- **A stopped instance runs no automated backup.** The backup window is
  12:00 UTC (09:00 local) for that reason, and not the small hours where a
  backup window normally goes. In the old windows the backups would simply stop
  happening, silently, with no error for anyone to notice.
- **There is no transaction log for the hours an instance was stopped**, so a
  restore to a point in time has to target a moment it was awake.

## What a project may do inside the shared instance

The custom role `sqlTenant`, granted to the identity that applies the project's
Terraform — which is not the identity that deploys its application. It may
create and alter its own database and its own users, and nothing else: no
`cloudsql.instances.delete`, no `cloudsql.instances.update`.

**That role is not a wall between projects.** Cloud SQL grants IAM per project
and not per database. What keeps one laboratory's data away from another's is
the database grant: `CONNECT` revoked from `PUBLIC` and given by name, with a
connection ceiling computed from that project's own pools.

That protection has a limit worth stating rather than implying away: a user
created by `gcloud sql users create` belongs to `cloudsqlsuperuser`, and a
member of that role can grant itself back the `CONNECT` this revokes. The
grant and the ceiling guard against **accident** — a runaway CI job, a
mistyped connection string — not against deliberate action from inside.
Between laboratories that share one owner, that is the accepted trade.
```

- [ ] **Step 2: Point the README at it**

Add to `README.md`, after `What this does not own`:

```markdown
## The rules of the laboratory

`docs/lab.md` — what may be shared, how things are named, when the instance is
awake, and what a project may do inside it.
```

- [ ] **Step 3: Move the earlier spec in**

```bash
git checkout -b claude/lab-rules
mkdir -p docs/superpowers/specs
cp ~/marketplace/docs/superpowers/specs/2026-09-20-shared-database-instance-design.md \
   docs/superpowers/specs/
git add docs/lab.md README.md docs/superpowers/specs/
git commit
git push -u origin claude/lab-rules
```

- [ ] **Step 4: Correct the pointers in the marketplace, in its own pull request**

Two files reference the moved spec or the old repository name. Find them rather
than trusting this list:

```bash
cd ~/marketplace
grep -rn 'shared-infra\|2026-09-20-shared-database-instance-design' \
  --include='*.md' docs/ README.md
```

For each hit: `aleogr/shared-infra` becomes `aleogr/lab`, and a link to the
spec's old path becomes its new home in `aleogr/lab`. Then delete
`docs/superpowers/specs/2026-09-20-shared-database-instance-design.md` from the
marketplace, in the same pull request that corrects the links — a deletion
landing before the links are fixed leaves a broken reference in `main`.

- [ ] **Step 5: Prove nothing still points at the old name**

```bash
cd ~/marketplace && grep -rn 'aleogr/shared-infra' --include='*.md' --include='*.tf' . | grep -v '^./.git'
```

Expected: no output. One exception is legitimate and must stay: any sentence
that describes the rename itself as history.

---

### Task 6: The marketplace lets the page read its health

**Files:**
- Modify: `internal/platform/httpx/httpx.go`
- Test: `internal/platform/httpx/httpx_test.go`

This is a pull request in `aleogr/marketplace`, independent of every task above.
It can land first; until it does, the marketplace reads as unknown on the page.

**Interfaces:**
- Consumes: nothing.
- Produces: `/health` answers with `Access-Control-Allow-Origin: https://lab.aleogr.dev`.

- [ ] **Step 1: Write the failing test**

In `internal/platform/httpx/httpx_test.go`:

```go
// The laboratory's index reads this endpoint from a reader's browser, which the
// same-origin policy refuses without this header. It is on /health and on
// nothing else: the endpoint is already public and answers with a status, a
// version and whether the database replies, none of which is a secret.
func TestHealthLetsTheLabIndexReadIt(t *testing.T) {
	recorder := httptest.NewRecorder()
	routes(t, nil).ServeHTTP(recorder, httptest.NewRequestWithContext(
		t.Context(), http.MethodGet, "/health", nil))

	if got := recorder.Header().Get("Access-Control-Allow-Origin"); got != "https://lab.aleogr.dev" {
		t.Errorf("Access-Control-Allow-Origin = %q, want %q", got, "https://lab.aleogr.dev")
	}
}

func TestOnlyHealthLetsTheLabIndexReadIt(t *testing.T) {
	recorder := httptest.NewRecorder()
	routes(t, nil).ServeHTTP(recorder, httptest.NewRequestWithContext(
		t.Context(), http.MethodGet, "/no-such-page", nil))

	if got := recorder.Header().Get("Access-Control-Allow-Origin"); got != "" {
		t.Errorf("Access-Control-Allow-Origin = %q on a path that is not the health check", got)
	}
}
```

The tests spell the origin out rather than referring to the constant on
purpose: a test that reads its expectation from the code under test would pass
no matter what that code said.

- [ ] **Step 2: Run them and watch the first fail**

```bash
go test ./internal/platform/httpx/ -run 'LabIndex' -v
```

Expected: `TestHealthLetsTheLabIndexReadIt` FAILS with
`Access-Control-Allow-Origin = "" , want "https://lab.aleogr.dev"`, and
`TestOnlyHealthLetsTheLabIndexReadIt` passes — it passes because nothing sets the
header anywhere yet, which is exactly what makes it worth keeping afterwards.

- [ ] **Step 3: Set the header**

In `internal/platform/httpx/httpx.go`, above `HealthPath`:

```go
// LabOrigin is the laboratory's index, which reads HealthPath from a reader's
// browser (aleogr/lab). The same-origin policy refuses that read unless the
// answer names the origin allowed to make it.
//
// A production deployment answers with it too, and that is harmless: the header
// grants the right to read a status, a version and whether the database
// replies, to one page, and grants nothing else to anyone.
const LabOrigin = "https://lab.aleogr.dev"
```

In `health`, beside the `Content-Type` line:

```go
		w.Header().Set("Access-Control-Allow-Origin", LabOrigin)
```

- [ ] **Step 4: Run the tests again**

```bash
go test ./internal/platform/httpx/ -run 'LabIndex' -v
```

Expected: both PASS.

- [ ] **Step 5: Run what CI runs, before pushing**

```bash
make check
make test
```

Expected: both clean. `make check` includes `gosec`, which is the tool most
likely to have an opinion about a header like this one; if it objects, read what
it says rather than silencing it.

- [ ] **Step 6: Commit, push, open the pull request**

```bash
git checkout -b claude/funny-wright-379asb-labcors
git add internal/platform/httpx/httpx.go internal/platform/httpx/httpx_test.go
git commit
git push -u origin claude/funny-wright-379asb-labcors
```

- [ ] **Step 7: Prove it in the deployed service, not only in the test**

After the owner merges and the deployment finishes:

```bash
curl -sSI https://marketplace.lab.aleogr.dev/health | grep -i access-control-allow-origin
```

Expected: `access-control-allow-origin: https://lab.aleogr.dev`. Then reload
`https://lab.aleogr.dev` and confirm the marketplace card has moved from
**unknown** to **up**, with its version beside it. That is the whole design
working end to end, and it is the only evidence that counts.

---

## Order and what may run in parallel

1 → 2 → 3 is a chain; no step may start before the one before it is verified.

Task 4 may be written at any time but published only after Task 2. Task 5 may
follow Task 3. Task 6 is independent of all of them and may go first.
