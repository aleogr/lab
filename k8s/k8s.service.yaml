apiVersion: v1
kind: Service
metadata:
  name: deployment-dockerapp
  labels:
    app: dockerapp
spec:
  type: NodePort
  selector:
    app: dockerapp
  ports:
    - name: http
      nodePort: 32767
      port: 8080
